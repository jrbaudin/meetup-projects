const UserDevices = `
    # These are the users registered devices
    type UserDevices {
        uuid: String
        platform: String
        created_date: String
    }
`

export default UserDevices
