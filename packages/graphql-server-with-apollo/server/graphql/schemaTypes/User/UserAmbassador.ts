const UserAmbassador = `
    # This is some basic information if the User is an Ambassador
    type UserAmbassador {
        id: Int!
        created_date: String
    }
`

export default UserAmbassador
