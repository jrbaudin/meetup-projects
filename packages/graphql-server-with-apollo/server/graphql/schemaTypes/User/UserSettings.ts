const UserSettings = `
    # These are the users notification settings
    type UserSettings {
        no_notifications: Boolean
        silent_notifications: Boolean
        premiere_notifications: Boolean
        notifications_from_followed_locations: Boolean
    }
`

export default UserSettings
